import { Platform } from '@expo/config';
export declare function getStandaloneBuilds({ id, platform, limit, slug, }: {
    id?: string;
    platform: Platform;
    limit: number;
    slug: string;
}): Promise<any>;
